#include <stdio.h>
#include "SDL2/SDL.h"

int main() {
    // if (SDL_Init(SDL_INIT_EVERYTHING) != 0) {
    //     printf("SDL_Init Error: %s\n", SDL_GetError());
    //     return 1;
    // }
    // SDL_Quit();

    return 0;
}
